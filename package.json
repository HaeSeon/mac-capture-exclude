{
  "name": "mac-capture-exclude",
  "version": "1.0.0",
  "description": "Native addon for capturing screen excluding StepHow windows",
  "main": "index.js",
  "scripts": {
    "install": "node-gyp rebuild",
    "build": "node-gyp rebuild"
  },
  "dependencies": {
    "node-addon-api": "^5.0.0"
  },
  "devDependencies": {
    "node-gyp": "^9.0.0"
  }
}
